import { OnpicksCurrencyPipe } from './onpicks-currency.pipe';

describe('OnpicksCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new OnpicksCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
