import { CleanLoadDirective } from './clean-load.directive';

describe('CleanLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new CleanLoadDirective();
    expect(directive).toBeTruthy();
  });
});
