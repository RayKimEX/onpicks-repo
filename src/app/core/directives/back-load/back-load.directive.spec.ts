import { BackLoadDirective } from './back-load.directive';

describe('BackLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new BackLoadDirective();
    expect(directive).toBeTruthy();
  });
});
