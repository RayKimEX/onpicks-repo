import { FloatingCheckoutDirective } from './floating-checkout.directive';

describe('FloatingCheckoutDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingCheckoutDirective();
    expect(directive).toBeTruthy();
  });
});
